import { LatLngBounds, LayerGroup, Map, PathOptions, Rectangle } from 'leaflet';
import { BaseLayer } from '../../core/base-layer';
export declare class RectangleComponent extends BaseLayer<Rectangle> {
    bounds: LatLngBounds;
    options: PathOptions;
    private _bounds;
    private _options;
    createLayer(): void;
    addTo(map: Map | LayerGroup): void;
}
