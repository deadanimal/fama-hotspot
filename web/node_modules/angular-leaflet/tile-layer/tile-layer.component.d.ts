import { LayerGroup, Map, TileLayer } from 'leaflet';
import { BaseLayer } from '../core/base-layer';
export declare class TileLayerComponent extends BaseLayer<TileLayer> {
    addTo(map: Map | LayerGroup): void;
}
