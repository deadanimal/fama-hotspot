import { Layer, Map } from 'leaflet';
export declare class LayerHandler<T extends Layer> {
    id: string;
    mapRef: Map;
    layerRef: T;
    constructor();
    initialize(mapRef: Map, layerRef: T): void;
}
