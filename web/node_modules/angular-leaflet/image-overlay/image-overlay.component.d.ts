import { ImageOverlay, LayerGroup, Map } from 'leaflet';
import { BaseLayer } from '../core/base-layer';
export declare class ImageOverlayComponent extends BaseLayer<ImageOverlay> {
    src: string;
    width: number;
    height: number;
    private _url;
    private _width;
    private _height;
    addTo(map: Map | LayerGroup): void;
    prepareLayer(): void;
    private prepareBounds;
}
