import { LatLngExpression, LayerGroup, Map, PathOptions, Polygon } from 'leaflet';
import { BaseLayer } from '../../core/base-layer';
export declare class PolygonComponent extends BaseLayer<Polygon> {
    coordinates: LatLngExpression[];
    options: PathOptions;
    private _coordinates;
    private _options;
    createLayer(): void;
    addTo(map: Map | LayerGroup): void;
}
